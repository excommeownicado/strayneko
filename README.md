# oneko-wander

A fork of oneko with a wander mode

# usage

* `oneko -wander` enables wander mode
* `oneko -wander-min-wait` and `oneko -wander-max-wait` sets min and max idle animation time between walks
